import React, { FC } from "react";

type FilesSectionProps = {
  section: "Files";
};

const FilesSection: FC<FilesSectionProps> = ({ type }) => {
  return <div>FilesSection</div>;
};

export default FilesSection;
